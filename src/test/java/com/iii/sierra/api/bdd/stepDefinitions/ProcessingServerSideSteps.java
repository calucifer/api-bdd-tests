package com.iii.sierra.api.bdd.stepDefinitions;

import org.jbehave.core.annotations.Given;
import org.jbehave.core.annotations.Then;
import org.jbehave.core.annotations.When;

/**
 * Created by ssatelle on 04/11/14.
 * for the project api-bdd-tests
 */
public class ProcessingServerSideSteps {

    @Given("my server is configured as a basic install")
    @Then("my server is configured as a basic install")
    public void serverIsBasicInstall() {

    }

    @Given("my server is configured as a premium install")
    @Then("my server is configured as a premium install")
    public void serverIsPremiumInstall() {

    }

    @When("I configure the server as a premium install")
    public void configureServerAsPremium() {

    }

    @When("I configure the server as a basic install")
    public void configureServerAsBasic() {

    }
}
